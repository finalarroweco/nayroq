"use client";import{useEffect,useState}from"react";import Link from"next/link";import{createClient}from"@/lib/supabase/client";export default function Manager(){const[data,setData]=useState<any>(null);useEffect(()=>{(async()=>{const s=createClient();const{data:{user}}=await s.auth.getUser();if(!user)return location.href="/login";const{data:c}=await s.from("companies").select("*").eq("owner_id",user.id).limit(1).single();const[{count:conversations},{data:leads},{data:quotes},{data:handoffs},{data:team},{data:followups}]=await Promise.all([s.from("conversations").select("*",{count:"exact",head:true}).eq("company_id",c.id),s.from("leads").select("*,contacts(name,phone),ai_employees(name)").eq("company_id",c.id),s.from("quotations").select("*").eq("company_id",c.id),s.from("conversations").select("id,channel,created_at,contacts(name,phone),ai_employees(name)").eq("company_id",c.id).eq("human_takeover",true),s.from("ai_employees").select("id,name,role,status").eq("company_id",c.id),s.from("follow_ups").select("*").eq("company_id",c.id).eq("status","pending")]);const L=leads||[];setData({company:c,conversations:conversations||0,leads:L.length,qualified:L.filter(x=>x.stage==="qualified").length,quotes:(quotes||[]).length,wins:L.filter(x=>x.stage==="won").length,lost:L.filter(x=>x.stage==="lost").length,revenue:L.filter(x=>x.stage==="won").reduce((a,x)=>a+Number(x.value||0),0),handoffs:handoffs||[],team:team||[],followups:followups||[],hot:L.filter(x=>!["won","lost"].includes(x.stage)).slice(0,5)})})()},[]);if(!data)return <main className="loading">Preparing your AI Manager brief…</main>;return <main className="module"><header><Link href="/dashboard">← Dashboard</Link><div className="logo"><i>N</i><b>NAYROQ</b></div><span>AI Manager</span></header><section><div className="eyebrow">EXECUTIVE OVERVIEW</div><h1>Good morning. Here’s what your AI team is doing.</h1><p>{data.team.filter((x:any)=>x.status==="active").length} active AI employees · {data.followups.length} follow-ups scheduled · {data.handoffs.length} conversations need human attention</p><div className="managerstats">{[["Conversations",data.conversations],["Leads",data.leads],["Qualified",data.qualified],["Quotations",data.quotes],["Won",data.wins],["Revenue",data.revenue+" "+data.company.currency]].map(x=><div className="stat" key={x[0]}><span>{x[0]}</span><strong>{x[1]}</strong></div>)}</div><div className="managergrid"><div className="panel"><h3>Sales Funnel</h3>{[["Conversations",data.conversations],["Leads",data.leads],["Qualified",data.qualified],["Quotations",data.quotes],["Won",data.wins]].map((x,i)=><div className="funnel" key={x[0]}><span>{x[0]}</span><div><i style={{width:Math.max(5,100-i*17)+"%"}}/></div><b>{x[1]}</b></div>)}</div><div className="panel"><h3>Needs Human Attention <em>{data.handoffs.length}</em></h3>{data.handoffs.length?data.handoffs.slice(0,6).map((x:any)=><div className="attention" key={x.id}><div><b>{x.contacts?.name||x.contacts?.phone||"Customer"}</b><small>{x.channel} · AI: {x.ai_employees?.name||"—"}</small></div><Link href="/inbox">Open →</Link></div>):<div className="placeholder">Nothing needs human attention right now.</div>}</div><div className="panel"><h3>Open Opportunities</h3>{data.hot.length?data.hot.map((x:any)=><div className="attention" key={x.id}><div><b>{x.contacts?.name||x.contacts?.phone||"Lead"}</b><small>{x.stage} · {x.value||0} {x.currency}</small></div><Link href={"/leads/"+x.id}>Review →</Link></div>):<div className="placeholder">No open opportunities.</div>}</div><div className="panel"><h3>AI Team</h3>{data.team.map((x:any)=><div className="attention" key={x.id}><div><b>{x.name}</b><small>{x.role.replace("_"," ")}</small></div><em className="online">● {x.status}</em></div>)}</div></div></section></main>