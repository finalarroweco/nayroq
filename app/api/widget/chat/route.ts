import{NextResponse}from"next/server";import{createClient}from"@supabase/supabase-js";export async function POST(req:Request){try{const{key,message,conversationId}=await req.json();const s=createClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.SUPABASE_SERVICE_ROLE_KEY!);const{data:c}=await s.from("companies").select("*").eq("widget_key",key).maybeSingle();if(!c)return NextResponse.json({error:"Invalid widget"},{status:404});const{data:emp}=await s.from("ai_employees").select("*").eq("company_id",c.id).eq("status","active").order("created_at").limit(1).maybeSingle();if(!emp)return NextResponse.json({error:"No active AI employee"},{status:409});let cid=conversationId;if(!cid){const{data:x}=await s.from("conversations").insert({company_id:c.id,ai_employee_id:emp.id,channel:"website",status:"open"}).select("id").single();cid=x?.id}if(!cid)return NextResponse.json({error:"Could not create conversation"},{status:500});await s.from("messages").insert({conversation_id:cid,sender:"customer",content:message});const[{data:k},{data:h},{data:conv}]=await Promise.all([s.from("knowledge_items").select("title,content").eq("company_id",c.id),s.from("messages").select("sender,content").eq("conversation_id",cid).order("created_at",{ascending:false}).limit(12),s.from("conversations").select("human_takeover").eq("id",cid).single()]);if(conv?.human_takeover)return NextResponse.json({conversationId:cid,text:"A team member will continue this conversation shortly.",handoff:true});const knowledge=(k||[]).map((x:any)=>"### "+x.title+"\n"+x.content).join("\n\n");const history=(h||[]).reverse().map((x:any)=>x.sender+": "+x.content).join("\n");const r=await fetch("https://api.openai.com/v1/responses",{method:"POST",headers:{"Content-Type":"application/json","Authorization":"Bearer "+process.env.OPENAI_API_KEY},body:JSON.stringify({model:process.env.OPENAI_MODEL||"gpt-5.6-luna",instructions:`You are ${emp.name}, an AI ${emp.role} employee for ${c.name}. Goal: ${emp.goal||"help the customer"}. Tone: ${emp.tone}. Reply in the customer's language. Use only approved company knowledge for factual business claims. Never invent prices, policies or services. Keep website chat replies concise and natural.\n\nAPPROVED KNOWLEDGE:\n${knowledge||"No approved knowledge."}`,input:history,reasoning:{effort:"none"}})});const d=await r.json();let answer=d.output_text||"";if(!answer)return NextResponse.json({error:d?.error?.message||"AI failed"},{status:500});await s.from("messages").insert({conversation_id:cid,sender:"ai",content:answer});return NextResponse.json({conversationId:cid,text:answer,employee:emp.name})}catch(e:any){return NextResponse.json({error:e.message||"Server error"},{status:500})}}