import{NextResponse}from"next/server";export async function GET(req:Request){const u=new URL(req.url);const code=u.searchParams.get("code");const error=u.searchParams.get("error");const target=new URL("/integrations",u.origin);if(error)target.searchParams.set("instagram","error");else if(code){target.searchParams.set("instagram","authorized");target.searchParams.set("code_received","1")}else target.searchParams.set("instagram","cancelled");return NextResponse.redirect(target)}