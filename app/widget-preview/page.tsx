"use client";import{useEffect,useState}from"react";import Link from"next/link";import{createClient}from"@/lib/supabase/client";export default function Preview(){const[key,setKey]=useState("");useEffect(()=>{(async()=>{const s=createClient();const{data:{user}}=await s.auth.getUser();if(!user){location.href="/login";return}const{data:c}=await s.from("companies").select("widget_key").eq("owner_id",user.id).limit(1).single();if(c?.widget_key)setKey(c.widget_key)})()},[]);useEffect(()=>{if(!key)return;const old=document.getElementById("nayroq-widget-preview");if(old)old.remove();const script=document.createElement("script");script.id="nayroq-widget-preview";script.src="/widget.js?key="+key;script.async=true;document.body.appendChild(script);return()=>{script.remove();document.querySelectorAll(".nq-btn,.nq-box").forEach(x=>x.remove())}},[key]);return <main className="previewpage"><header><Link href="/integrations">← Integrations</Link><div className="logo"><i>N</i><b>NAYROQ</b></div><span>Widget Preview</span></header><section><div className="eyebrow">LIVE PREVIEW</div><h1>Test your website AI employee.</h1><p>This page simulates a customer visiting your website. Click the N button in the bottom-right corner and start a conversation.</p><div className="previewsite"><nav><b>Your Company</b><span>Home&nbsp;&nbsp;&nbsp; Services&nbsp;&nbsp;&nbsp; Contact</span></nav><div><small>WELCOME</small><h2>Grow your business with a team that never stops.</h2><p>This is a sample website used only to preview your NAYROQ chat widget.</p><button>Explore Services</button></div></div><div className="testnote"><b>What to test</b><span>Ask about a service or price from your Knowledge Base. Then open Unified Inbox and confirm the website conversation appears there.</span></div></section></main>}