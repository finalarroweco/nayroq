"use client";import{FormEvent,useState}from"react";import Link from"next/link";import{createClient}from"@/lib/supabase/client";import{LanguageSwitch,useLanguage}from"@/app/i18n";export default function Reset(){const{lang}=useLanguage();const[password,setPassword]=useState("");const[msg,setMsg]=useState("");async function submit(e:FormEvent){e.preventDefault();setMsg(lang==="ar"?"جاري تحديث كلمة المرور…":"Updating password…");const{error}=await createClient().auth.updateUser({password});if(error)return setMsg(error.message);setMsg(lang==="ar"?"تم تغيير كلمة المرور. يمكنك تسجيل الدخول الآن.":"Password updated. You can sign in now.")}return <main className="auth"><LanguageSwitch/><Link href="/" className="logo"><i>N</i><b>NAYROQ</b></Link><form onSubmit={submit}><div className="eyebrow">{lang==="ar"?"كلمة مرور جديدة":"NEW PASSWORD"}</div><h1>{lang==="ar"?"عيّن كلمة مرور جديدة":"Set a new password"}</h1><label>{lang==="ar"?"كلمة المرور الجديدة":"New password"}<input type="password" minLength={8} required value={password} onChange={e=>setPassword(e.target.value)} placeholder={lang==="ar"?"8 أحرف أو أكثر":"8+ characters"}/></label><button>{lang==="ar"?"تحديث كلمة المرور":"Update Password"}</button>{msg&&<small>{msg}</small>}<p><Link href="/login">{lang==="ar"?"الذهاب لتسجيل الدخول":"Go to sign in"}</Link></p></form></main>}