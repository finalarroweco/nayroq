"use client";import {FormEvent,useState} from "react";import {createClient} from "@/lib/supabase/client";import Link from "next/link";import{useLanguage,LanguageSwitch}from"@/app/i18n";
export default function Login(){const{t,lang}=useLanguage();const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[msg,setMsg]=useState("");async function submit(e:FormEvent){e.preventDefault();setMsg(lang==="ar"?"جاري تسجيل الدخول…":"Signing in…");const{error}=await createClient().auth.signInWithPassword({email,password});if(error)return setMsg(error.message);location.href="/dashboard"}return <main className="auth"><LanguageSwitch/><Link href="/" className="logo"><i>N</i><b>NAYROQ</b></Link><form onSubmit={submit}><div className="eyebrow">{t("welcome")}</div><h1>{t("login")}</h1><p>{lang==="ar"?"أدر فريق الذكاء الاصطناعي الخاص بك.":"Manage your AI workforce."}</p><label>{t("email")}<input type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@company.com"/></label><label>{t("password")}<input type="password" required value={password} onChange={e=>setPassword(e.target.value)} placeholder="••••••••"/></label><button>{t("signin")}</button>{msg&&<small>{msg}</small>}<p>{lang==="ar"?"جديد في NAYROQ؟ ":"New to NAYROQ? "}<Link href="/signup">{lang==="ar"?"إنشاء حساب":"Create account"}</Link></p></form></main>}