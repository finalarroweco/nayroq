"use client";import{useEffect,useState}from"react";import Link from"next/link";import{createClient}from"@/lib/supabase/client";export default function GettingStarted(){const[x,setX]=useState<any>();useEffect(()=>{(async()=>{const s=createClient();const{data:{user}}=await s.auth.getUser();if(!user){location.href="/login";return}const{data:c}=await s.from("companies").select("*").eq("owner_id",user.id).limit(1).single();if(!c)return;const[{count:e},{count:k},{count:ch}]=await Promise.all([s.from("ai_employees").select("*",{count:"exact",head:true}).eq("company_id",c.id),s.from("knowledge_items").select("*",{count:"exact",head:true}).eq("company_id",c.id),s.from("channel_connections").select("*",{count:"exact",head:true}).eq("company_id",c.id).eq("status","connected")]);setX({company:c,e:e||0,k:k||0,ch:ch||0})})()},[]);if(!x)return <main className="loading">Preparing setup…</main>;const steps=[["Company profile",!!x.company.name,"/settings","Confirm your company, market and currency."],["Hire an AI employee",x.e>0,"/employees/new","Choose a role, goal, language and tone."],["Add business knowledge",x.k>0,"/knowledge","Teach NAYROQ your services, prices and policies."],["Connect a channel",x.ch>0,"/integrations","Connect WhatsApp, Instagram or another channel."],["Test your AI",x.e>0&&x.k>0,"/dashboard","Open an employee and run a test conversation."]];const done=steps.filter(s=>s[1]).length;return <main className="module"><header><Link href="/dashboard">← Dashboard</Link><div className="logo"><i>N</i><b>NAYROQ</b></div><span>Getting Started</span></header><section><div className="eyebrow">SETUP</div><h1>Launch your AI workforce.</h1><p>{done} of {steps.length} steps completed</p><div className="setupbar"><i style={{width:(done/steps.length*100)+"%"}}/></div><div className="setupsteps">{steps.map((s:any,i)=><Link href={s[2]} className={s[1]?"done":""} key={s[0]}><i>{s[1]?"✓":i+1}</i><div><b>{s[0]}</b><p>{s[3]}</p></div><span>{s[1]?"Complete":"Continue →"}</span></Link>)}</div></section></main>