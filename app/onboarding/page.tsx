"use client";import {FormEvent,useState} from "react";import {createClient} from "@/lib/supabase/client";
export default function Onboarding(){const[name,setName]=useState("");const[industry,setIndustry]=useState("");const[country,setCountry]=useState("Oman");const[msg,setMsg]=useState("");async function submit(e:FormEvent){e.preventDefault();const s=createClient();const{data:{user}}=await s.auth.getUser();if(!user){location.href="/login";return}const{error}=await s.from("companies").insert({owner_id:user.id,name,industry,country,currency:country==="Saudi Arabia"?"SAR":country==="UAE"?"AED":"OMR"});if(error)return setMsg(error.message);location.href="/dashboard"}return <main className="auth"><div className="logo"><i>N</i><b>NAYROQ</b></div><form onSubmit={submit}><div className="eyebrow">STEP 1 OF 2</div><h1>Tell us about your business</h1><p>We’ll use this to configure your AI workforce.</p><label>Company name<input required value={name} onChange={e=>setName(e.target.value)} placeholder="Acme Company"/></label><label>Industry<input required value={industry} onChange={e=>setIndustry(e.target.value)} placeholder="Real estate, salon, retail…"/></label><label>Country<select value={country} onChange={e=>setCountry(e.target.value)}><option>Oman</option><option>Saudi Arabia</option><option>UAE</option><option>Kuwait</option><option>Qatar</option><option>Bahrain</option></select></label><button>Continue →</button>{msg&&<small>{msg}</small>}</form></main>}