"use client";import {FormEvent,useState} from "react";import {createClient} from "@/lib/supabase/client";import Link from "next/link";
export default function Signup(){const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[msg,setMsg]=useState("");async function submit(e:FormEvent){e.preventDefault();setMsg("Creating account…");const{error}=await createClient().auth.signUp({email,password,options:{emailRedirectTo:location.origin+"/onboarding"}});if(error)return setMsg(error.message);setMsg("Check your email to confirm your account.")}return <main className="auth"><Link href="/" className="logo"><i>N</i><b>NAYROQ</b></Link><form onSubmit={submit}><div className="eyebrow">START BUILDING YOUR TEAM</div><h1>Create your account</h1><p>Your first AI employee is minutes away.</p><label>Work email<input type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@company.com"/></label><label>Password<input type="password" minLength={8} required value={password} onChange={e=>setPassword(e.target.value)} placeholder="8+ characters"/></label><button>Create Account →</button>{msg&&<small>{msg}</small>}<p>Already have an account? <Link href="/login">Sign in</Link></p></form></main>}