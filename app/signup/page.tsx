"use client";import {FormEvent,useState} from "react";import {createClient} from "@/lib/supabase/client";import Link from "next/link";import{useLanguage,LanguageSwitch}from"@/app/i18n";
export default function Signup(){const{t,lang}=useLanguage();const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[msg,setMsg]=useState("");async function submit(e:FormEvent){e.preventDefault();setMsg(lang==="ar"?"جاري إنشاء الحساب…":"Creating account…");const{error}=await createClient().auth.signUp({email,password,options:{emailRedirectTo:(process.env.NEXT_PUBLIC_APP_URL||window.location.origin)+"/onboarding"}});if(error)return setMsg(error.message);setMsg(lang==="ar"?"تحقق من بريدك الإلكتروني لتأكيد الحساب.":"Check your email to confirm your account.")}return <main className="auth"><LanguageSwitch/><Link href="/" className="logo"><i>N</i><b>NAYROQ</b></Link><form onSubmit={submit}><div className="eyebrow">{lang==="ar"?"ابدأ ببناء فريقك":"START BUILDING YOUR TEAM"}</div><h1>{t("createAccount")}</h1><p>{lang==="ar"?"أول موظف AI على بعد دقائق.":"Your first AI employee is minutes away."}</p><label>{t("workEmail")}<input type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@company.com"/></label><label>{t("password")}<input type="password" minLength={8} required value={password} onChange={e=>setPassword(e.target.value)} placeholder={lang==="ar"?"8 أحرف أو أكثر":"8+ characters"}/></label><button>{t("create")} →</button>{msg&&<small>{msg}</small>}<p>{lang==="ar"?"لديك حساب بالفعل؟ ":"Already have an account? "}<Link href="/login">{lang==="ar"?"تسجيل الدخول":"Sign in"}</Link></p></form></main>}