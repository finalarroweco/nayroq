"use client";import{FormEvent,useState}from"react";import Link from"next/link";import{createClient}from"@/lib/supabase/client";import{LanguageSwitch,useLanguage}from"@/app/i18n";export default function Forgot(){const{lang}=useLanguage();const[email,setEmail]=useState("");const[msg,setMsg]=useState("");async function submit(e:FormEvent){e.preventDefault();setMsg(lang==="ar"?"جاري إرسال الرابط…":"Sending reset link…");const redirect=(process.env.NEXT_PUBLIC_APP_URL||window.location.origin)+"/reset-password";const{error}=await createClient().auth.resetPasswordForEmail(email,{redirectTo:redirect});setMsg(error?error.message:(lang==="ar"?"تم إرسال رابط إعادة تعيين كلمة المرور إلى بريدك.":"Password reset link sent. Check your email."))}return <main className="auth"><LanguageSwitch/><Link href="/" className="logo"><i>N</i><b>NAYROQ</b></Link><form onSubmit={submit}><div className="eyebrow">{lang==="ar"?"استعادة الحساب":"ACCOUNT RECOVERY"}</div><h1>{lang==="ar"?"نسيت كلمة المرور؟":"Forgot your password?"}</h1><p>{lang==="ar"?"أدخل بريدك وسنرسل لك رابطًا لتعيين كلمة مرور جديدة.":"Enter your email and we’ll send you a link to set a new password."}</p><label>{lang==="ar"?"البريد الإلكتروني":"Email"}<input type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@company.com"/></label><button>{lang==="ar"?"إرسال رابط الاستعادة":"Send Reset Link"}</button>{msg&&<small>{msg}</small>}<p><Link href="/login">{lang==="ar"?"العودة لتسجيل الدخول":"Back to sign in"}</Link></p></form></main>}